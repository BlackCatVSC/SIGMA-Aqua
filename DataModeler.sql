CREATE TABLE EMPRESA (
    ID_EMPRESA NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_EMPRESA VARCHAR2(100) NOT NULL,
    RUT_EMPRESA VARCHAR2(13) NOT NULL UNIQUE,
    DIRECCION_EMPRESA VARCHAR2(100),
    TELEFONO_EMPRESA VARCHAR2(12),
    EMAIL_EMPRESA VARCHAR2(50)
);

CREATE TABLE ZONA_SANITARIA (
    ID_ZONA NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CODIGO_OFICIAL_ZONA VARCHAR2(20) NOT NULL UNIQUE,
    NOMBRE_ZONA VARCHAR2(100) NOT NULL,
    REGION_ZONA VARCHAR2(50),
    PROVINCIA_ZONA VARCHAR2(50),
    ESTADO_ZONA VARCHAR2(30)
);

CREATE TABLE CENTRO_CULTIVO (
    ID_CENTRO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_CENTRO VARCHAR2(100) NOT NULL,
    UBICACION_CENTRO VARCHAR2(100) NOT NULL,
    VOLUMEN_CENTRO_M3 NUMBER NOT NULL,
    DENSIDAD_MAXIMA_KG_M3 NUMBER NOT NULL,
    CAPACIDAD_MAX_CENTRO NUMBER,
    TIPO_AGUA_CENTRO VARCHAR2(30),
    PROFUNDIDAD_CENTRO NUMBER,
    FECHA_INICIO_CENTRO DATE,
    CODIGO_PERMISO_CONCESION_CENTRO VARCHAR2(50),
    ESTADO_CENTRO VARCHAR2(10),
    ID_EMPRESA NUMBER NOT NULL,
    ID_ZONA NUMBER,
    CONSTRAINT FK_CENTRO_EMPRESA
        FOREIGN KEY (ID_EMPRESA)
        REFERENCES EMPRESA(ID_EMPRESA),
    CONSTRAINT FK_CENTRO_ZONA
        FOREIGN KEY (ID_ZONA)
        REFERENCES ZONA_SANITARIA(ID_ZONA)
);

CREATE TABLE ESPECIE (
    ID_ESPECIE NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_COMUN_ESPECIE VARCHAR2(50) NOT NULL,
    NOMBRE_CIENTIFICO_ESPECIE VARCHAR2(100) NOT NULL
);

CREATE TABLE ROL (
    ID_ROL NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_ROL VARCHAR2(50) NOT NULL,
    DESCRIPCION_ROL VARCHAR2(200)
);

CREATE TABLE PERMISO (
    ID_PERMISO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_PERMISO VARCHAR2(50) NOT NULL,
    DESCRIPCION_PERMISO VARCHAR2(200)
);

CREATE TABLE ROL_PERMISO (
    ID_ROL NUMBER NOT NULL,
    ID_PERMISO NUMBER NOT NULL,
    CONSTRAINT PK_ROL_PERMISO PRIMARY KEY (ID_ROL, ID_PERMISO),
    CONSTRAINT FK_RP_ROL
        FOREIGN KEY (ID_ROL)
        REFERENCES ROL(ID_ROL),
    CONSTRAINT FK_RP_PERMISO
        FOREIGN KEY (ID_PERMISO)
        REFERENCES PERMISO(ID_PERMISO)
);

CREATE TABLE TRABAJADOR (
    ID_TRABAJADOR NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_TRAB VARCHAR2(50) NOT NULL,
    APELLIDO_TRAB VARCHAR2(50) NOT NULL,
    RUT_TRAB VARCHAR2(12) NOT NULL UNIQUE,
    CARGO_TRAB VARCHAR2(50),
    TELEFONO_TRAB VARCHAR2(12),
    EMAIL_TRAB VARCHAR2(50),
    ESTADO_TRAB VARCHAR2(10),
    FECHA_CONTRATO_TRAB DATE,
    ID_CENTRO NUMBER NOT NULL,
    CONSTRAINT FK_TRABAJADOR_CENTRO
        FOREIGN KEY (ID_CENTRO)
        REFERENCES CENTRO_CULTIVO(ID_CENTRO)
);

CREATE TABLE VETERINARIO (
    ID_VETERINARIO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    RUT_VET VARCHAR2(12) NOT NULL UNIQUE,
    REGISTRO_PROFESIONAL_VET VARCHAR2(20) NOT NULL,
    NOMBRE_VET VARCHAR2(50) NOT NULL,
    APELLIDO_VET VARCHAR2(50) NOT NULL,
    EMAIL_VET VARCHAR2(50),
    TELEFONO_VET VARCHAR2(12),
    TIPO_CONTRATO_VET VARCHAR2(20),
    ESPECIALIDAD_VET VARCHAR2(50),
    FIRMA_DIGITAL_VET BLOB,
    ESTADO_VET VARCHAR2(20) NOT NULL,
    FECHA_CONTRATO_VET DATE NOT NULL
);

CREATE TABLE VETERINARIO_CENTRO (
    ID_VETERINARIO NUMBER NOT NULL,
    ID_CENTRO NUMBER NOT NULL,
    FECHA_INICIO_VET_CENTRO DATE,
    FECHA_FIN_VET_CENTRO DATE,
    CONSTRAINT PK_VET_CENTRO PRIMARY KEY (ID_VETERINARIO, ID_CENTRO),
    CONSTRAINT FK_VC_VET
        FOREIGN KEY (ID_VETERINARIO)
        REFERENCES VETERINARIO(ID_VETERINARIO),
    CONSTRAINT FK_VC_CENTRO
        FOREIGN KEY (ID_CENTRO)
        REFERENCES CENTRO_CULTIVO(ID_CENTRO)
);

CREATE TABLE PROVEEDOR (
    ID_PROVEEDOR NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_PROV VARCHAR2(100) NOT NULL,
    RUT_PROV VARCHAR2(13) NOT NULL UNIQUE,
    DIRECCION_PROV VARCHAR2(100),
    TELEFONO_PROV VARCHAR2(12),
    EMAIL_PROV VARCHAR2(50),
    TIPO_PROV VARCHAR2(30)
);

CREATE TABLE USUARIO (
    ID_USUARIO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    EMAIL_USU VARCHAR2(50) NOT NULL UNIQUE,
    PASSWORD_HASH_USU VARCHAR2(200) NOT NULL,
    ESTADO_USU VARCHAR2(10),
    ID_ROL NUMBER NOT NULL,
    ID_TRABAJADOR NUMBER,
    ID_VETERINARIO NUMBER,
    ID_PROVEEDOR NUMBER,
    CONSTRAINT FK_USUARIO_ROL
        FOREIGN KEY (ID_ROL)
        REFERENCES ROL(ID_ROL),
    CONSTRAINT FK_USUARIO_TRAB
        FOREIGN KEY (ID_TRABAJADOR)
        REFERENCES TRABAJADOR(ID_TRABAJADOR),
    CONSTRAINT FK_USUARIO_VET
        FOREIGN KEY (ID_VETERINARIO)
        REFERENCES VETERINARIO(ID_VETERINARIO),
    CONSTRAINT FK_USUARIO_PROV
        FOREIGN KEY (ID_PROVEEDOR)
        REFERENCES PROVEEDOR(ID_PROVEEDOR)
);

CREATE TABLE LOTE (
    ID_LOTE NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FECHA_INGRESO_LOTE DATE NOT NULL,
    CANTIDAD_INICIAL_LOTE NUMBER NOT NULL,
    CANTIDAD_ACTUAL_LOTE NUMBER,
    ESTADO_LOTE VARCHAR2(20),
    ID_CENTRO NUMBER NOT NULL,
    ID_ESPECIE NUMBER NOT NULL,
    CONSTRAINT FK_LOTE_CENTRO
        FOREIGN KEY (ID_CENTRO)
        REFERENCES CENTRO_CULTIVO(ID_CENTRO),
    CONSTRAINT FK_LOTE_ESPECIE
        FOREIGN KEY (ID_ESPECIE)
        REFERENCES ESPECIE(ID_ESPECIE)
);

CREATE TABLE TIPO_ALIMENTO (
    ID_TIPO_ALIMENTO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_TIPO_ALIM VARCHAR2(50) NOT NULL,
    DESCRIPCION_TIPO_ALIM VARCHAR2(200)
);

CREATE TABLE ALIMENTACION (
    ID_ALIMENTACION NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FECHA_ALIM DATE NOT NULL,
    CANTIDAD_ALIM NUMBER NOT NULL,
    ID_LOTE NUMBER NOT NULL,
    ID_TIPO_ALIMENTO NUMBER NOT NULL,
    ID_TRABAJADOR NUMBER,
    CONSTRAINT FK_ALIM_LOTE
        FOREIGN KEY (ID_LOTE)
        REFERENCES LOTE(ID_LOTE),
    CONSTRAINT FK_ALIM_TIPO
        FOREIGN KEY (ID_TIPO_ALIMENTO)
        REFERENCES TIPO_ALIMENTO(ID_TIPO_ALIMENTO),
    CONSTRAINT FK_ALIM_TRAB
        FOREIGN KEY (ID_TRABAJADOR)
        REFERENCES TRABAJADOR(ID_TRABAJADOR)
);

CREATE TABLE MUESTREO (
    ID_MUESTREO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FECHA_MUES DATE NOT NULL,
    PESO_PROMEDIO_MUES NUMBER,
    CANTIDAD_MUESTRA_MUES NUMBER,
    ID_LOTE NUMBER NOT NULL,
    ID_TRABAJADOR NUMBER,
    CONSTRAINT FK_MUESTREO_LOTE
        FOREIGN KEY (ID_LOTE)
        REFERENCES LOTE(ID_LOTE),
    CONSTRAINT FK_MUESTREO_TRAB
        FOREIGN KEY (ID_TRABAJADOR)
        REFERENCES TRABAJADOR(ID_TRABAJADOR)
);

CREATE TABLE CAUSA_MORTALIDAD (
    ID_CAUSA NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_CAUSA VARCHAR2(100) NOT NULL,
    DESCRIPCION_CAUSA VARCHAR2(200)
);

CREATE TABLE MORTALIDAD (
    ID_MORTALIDAD NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FECHA_MORT DATE NOT NULL,
    CANTIDAD_MORT NUMBER NOT NULL,
    OBSERVACIONES_MORT VARCHAR2(300),
    ID_LOTE NUMBER NOT NULL,
    ID_CAUSA NUMBER NOT NULL,
    ID_TRABAJADOR NUMBER,
    CONSTRAINT FK_MORT_LOTE
        FOREIGN KEY (ID_LOTE)
        REFERENCES LOTE(ID_LOTE),
    CONSTRAINT FK_MORT_CAUSA
        FOREIGN KEY (ID_CAUSA)
        REFERENCES CAUSA_MORTALIDAD(ID_CAUSA),
    CONSTRAINT FK_MORT_TRAB
        FOREIGN KEY (ID_TRABAJADOR)
        REFERENCES TRABAJADOR(ID_TRABAJADOR)
);

CREATE TABLE ENFERMEDAD (
    ID_ENFERMEDAD NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_ENFERM VARCHAR2(100) NOT NULL,
    DESCRIPCION_ENFERM VARCHAR2(300),
    SINTOMAS_ENFERM VARCHAR2(300)
);

CREATE TABLE REGISTRO_SANITARIO (
    ID_REGISTRO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FECHA_REG_SANIT DATE NOT NULL,
    DIAGNOSTICO_REG_SANIT VARCHAR2(300),
    GRAVEDAD_REG_SANIT VARCHAR2(30),
    OBSERVACIONES_REG_SANIT VARCHAR2(300),
    ID_LOTE NUMBER NOT NULL,
    ID_ENFERMEDAD NUMBER NOT NULL,
    ID_VETERINARIO NUMBER,
    CONSTRAINT FK_RS_LOTE
        FOREIGN KEY (ID_LOTE)
        REFERENCES LOTE(ID_LOTE),
    CONSTRAINT FK_RS_ENFERMEDAD
        FOREIGN KEY (ID_ENFERMEDAD)
        REFERENCES ENFERMEDAD(ID_ENFERMEDAD),
    CONSTRAINT FK_RS_VET
        FOREIGN KEY (ID_VETERINARIO)
        REFERENCES VETERINARIO(ID_VETERINARIO)
);

CREATE TABLE PRODUCTO (
    ID_PRODUCTO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_PROD VARCHAR2(100) NOT NULL,
    TIPO_PROD VARCHAR2(30),
    UNIDAD_MEDIDA_PROD VARCHAR2(20),
    PRECIO_PROD NUMBER,
    STOCK_PROD NUMBER
);

CREATE TABLE TRATAMIENTO (
    ID_TRATAMIENTO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    DOSIS_TRAT VARCHAR2(50),
    FECHA_INICIO_TRAT DATE,
    FECHA_FIN_TRAT DATE,
    ESTADO_TRAT VARCHAR2(20),
    ID_REGISTRO NUMBER NOT NULL,
    ID_PRODUCTO NUMBER NOT NULL,
    ID_VETERINARIO NUMBER,
    CONSTRAINT FK_TRAT_REG
        FOREIGN KEY (ID_REGISTRO)
        REFERENCES REGISTRO_SANITARIO(ID_REGISTRO),
    CONSTRAINT FK_TRAT_PROD
        FOREIGN KEY (ID_PRODUCTO)
        REFERENCES PRODUCTO(ID_PRODUCTO),
    CONSTRAINT FK_TRAT_VET
        FOREIGN KEY (ID_VETERINARIO)
        REFERENCES VETERINARIO(ID_VETERINARIO)
);

CREATE TABLE EMBARCACION (
    ID_EMBARCACION NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_EMB VARCHAR2(50) NOT NULL,
    MATRICULA_EMB VARCHAR2(20) NOT NULL UNIQUE,
    TIPO_EMB VARCHAR2(30),
    CAPACIDAD_EMB NUMBER,
    ESTADO_EMB VARCHAR2(10),
    ID_CENTRO NUMBER,
    CONSTRAINT FK_EMB_CENTRO
        FOREIGN KEY (ID_CENTRO)
        REFERENCES CENTRO_CULTIVO(ID_CENTRO)
);

CREATE TABLE TRASLADO (
    ID_TRASLADO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FECHA_SALIDA_TRAS DATE,
    FECHA_LLEGADA_TRAS DATE,
    CANTIDAD_TRAS NUMBER,
    ESTADO_TRAS VARCHAR2(20),
    ID_LOTE NUMBER NOT NULL,
    ID_CENTRO_ORIGEN NUMBER NOT NULL,
    ID_CENTRO_DESTINO NUMBER NOT NULL,
    ID_EMBARCACION NUMBER,
    ID_TRABAJADOR NUMBER,
    CONSTRAINT FK_TRAS_LOTE
        FOREIGN KEY (ID_LOTE)
        REFERENCES LOTE(ID_LOTE),
    CONSTRAINT FK_TRAS_ORIGEN
        FOREIGN KEY (ID_CENTRO_ORIGEN)
        REFERENCES CENTRO_CULTIVO(ID_CENTRO),
    CONSTRAINT FK_TRAS_DESTINO
        FOREIGN KEY (ID_CENTRO_DESTINO)
        REFERENCES CENTRO_CULTIVO(ID_CENTRO),
    CONSTRAINT FK_TRAS_EMB
        FOREIGN KEY (ID_EMBARCACION)
        REFERENCES EMBARCACION(ID_EMBARCACION),
    CONSTRAINT FK_TRAS_TRAB
        FOREIGN KEY (ID_TRABAJADOR)
        REFERENCES TRABAJADOR(ID_TRABAJADOR)
);

CREATE TABLE PRODUCCION (
    ID_PRODUCCION NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FECHA_PRODUC DATE NOT NULL,
    CANTIDAD_PECES_PRODUC NUMBER,
    PESO_TOTAL_KG_PRODUC NUMBER,
    DESTINO_PRODUC VARCHAR2(30),
    OBSERVACIONES_PRODUC VARCHAR2(300),
    ID_LOTE NUMBER NOT NULL,
    ID_TRABAJADOR NUMBER,
    CONSTRAINT FK_PROD_LOTE
        FOREIGN KEY (ID_LOTE)
        REFERENCES LOTE(ID_LOTE),
    CONSTRAINT FK_PROD_TRAB
        FOREIGN KEY (ID_TRABAJADOR)
        REFERENCES TRABAJADOR(ID_TRABAJADOR)
);

CREATE TABLE COMPRA (
    ID_COMPRA NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FECHA_COMPRA DATE NOT NULL,
    TOTAL_COMPRA NUMBER,
    ESTADO_COMPRA VARCHAR2(20),
    ID_PROVEEDOR NUMBER NOT NULL,
    ID_CENTRO NUMBER,
    ID_TRABAJADOR NUMBER,
    CONSTRAINT FK_COMPRA_PROV
        FOREIGN KEY (ID_PROVEEDOR)
        REFERENCES PROVEEDOR(ID_PROVEEDOR),
    CONSTRAINT FK_COMPRA_CENTRO
        FOREIGN KEY (ID_CENTRO)
        REFERENCES CENTRO_CULTIVO(ID_CENTRO),
    CONSTRAINT FK_COMPRA_TRAB
        FOREIGN KEY (ID_TRABAJADOR)
        REFERENCES TRABAJADOR(ID_TRABAJADOR)
);

CREATE TABLE DETALLE_COMPRA (
    ID_DETALLE NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CANTIDAD_DET_COMPRA NUMBER NOT NULL,
    PRECIO_UNITARIO_DET_COMPRA NUMBER NOT NULL,
    ID_COMPRA NUMBER NOT NULL,
    ID_PRODUCTO NUMBER NOT NULL,
    CONSTRAINT FK_DET_COMPRA
        FOREIGN KEY (ID_COMPRA)
        REFERENCES COMPRA(ID_COMPRA),
    CONSTRAINT FK_DET_PROD
        FOREIGN KEY (ID_PRODUCTO)
        REFERENCES PRODUCTO(ID_PRODUCTO)
);

CREATE TABLE RECEPCION (
    ID_RECEPCION NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    FECHA_RECEP DATE,
    ESTADO_RECEP VARCHAR2(20),
    OBSERVACIONES_RECEP VARCHAR2(300),
    ID_COMPRA NUMBER NOT NULL,
    ID_TRABAJADOR NUMBER,
    CONSTRAINT FK_REC_COMPRA
        FOREIGN KEY (ID_COMPRA)
        REFERENCES COMPRA(ID_COMPRA),
    CONSTRAINT FK_REC_TRAB
        FOREIGN KEY (ID_TRABAJADOR)
        REFERENCES TRABAJADOR(ID_TRABAJADOR)
);

CREATE TABLE TIPO_EQUIPAMIENTO (
    ID_TIPO_EQUIPAMIENTO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_TIPO_EQUIP VARCHAR2(50) NOT NULL
);

CREATE TABLE EQUIPAMIENTO (
    ID_EQUIPAMIENTO NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    NOMBRE_EQUIP VARCHAR2(100) NOT NULL,
    MARCA_EQUIP VARCHAR2(50),
    MODELO_EQUIP VARCHAR2(50),
    ESTADO_EQUIP VARCHAR2(20),
    FECHA_ADQUISICION_EQUIP DATE,
    CANTIDAD_EQUIP NUMBER,
    ID_TIPO_EQUIPAMIENTO NUMBER NOT NULL,
    ID_CENTRO NUMBER NOT NULL,
    CONSTRAINT FK_EQUIP_TIPO
        FOREIGN KEY (ID_TIPO_EQUIPAMIENTO)
        REFERENCES TIPO_EQUIPAMIENTO(ID_TIPO_EQUIPAMIENTO),
    CONSTRAINT FK_EQUIP_CENTRO
        FOREIGN KEY (ID_CENTRO)
        REFERENCES CENTRO_CULTIVO(ID_CENTRO)
);

CREATE TABLE TAREA (
    ID_TAREA NUMBER GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    DESCRIPCION_TAREA VARCHAR2(300) NOT NULL,
    FECHA_ASIGNADA_TAREA DATE,
    FECHA_LIMITE_TAREA DATE,
    ESTADO_TAREA VARCHAR2(20),
    ID_CENTRO NUMBER NOT NULL,
    ID_LOTE NUMBER,
    ID_TRABAJADOR_ASIGNADO NUMBER,
    ID_TRABAJADOR_REGISTRA NUMBER,
    CONSTRAINT FK_TAREA_CENTRO
        FOREIGN KEY (ID_CENTRO)
        REFERENCES CENTRO_CULTIVO(ID_CENTRO),
    CONSTRAINT FK_TAREA_LOTE
        FOREIGN KEY (ID_LOTE)
        REFERENCES LOTE(ID_LOTE),
    CONSTRAINT FK_TAREA_ASIG
        FOREIGN KEY (ID_TRABAJADOR_ASIGNADO)
        REFERENCES TRABAJADOR(ID_TRABAJADOR),
    CONSTRAINT FK_TAREA_REG
        FOREIGN KEY (ID_TRABAJADOR_REGISTRA)
        REFERENCES TRABAJADOR(ID_TRABAJADOR)
);
